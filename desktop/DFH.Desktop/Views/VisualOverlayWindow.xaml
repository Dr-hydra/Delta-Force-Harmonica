<Window x:Class="DFH.Desktop.Views.VisualOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DFH.Desktop.Views"
        Title="口琴 · 可视化悬浮窗" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True" ShowActivated="False" ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip" MinWidth="440" MinHeight="260" Width="640" Height="460">
    <Border x:Name="OverlayBackground" Background="#C018252E" BorderBrush="#8295A9B6" BorderThickness="1" CornerRadius="9" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/><RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel Background="Transparent" MouseLeftButtonDown="DragHeader">
                <Button DockPanel.Dock="Right" Content="×" Click="CloseOverlay" Padding="9,0" ToolTip="关闭悬浮窗"/>
                <CheckBox x:Name="KeyLabelsToggle" DockPanel.Dock="Right" Content="键位提示" Margin="8,0" VerticalAlignment="Center" Foreground="White" Click="ToggleKeyLabels"
                          ToolTip="显示或隐藏音符上的文字提示和底部键位；颜色保持显示"/>
                <TextBlock x:Name="Heading" Foreground="White" Text="手动可视化 · 拖动这里移动" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            </DockPanel>
            <WrapPanel Grid.Row="1" Margin="0,7,0,7" ToolTip="到线按下，长条结束松开；绿色原调、蓝色降调、橙色升调、粉边半音。">
                <TextBlock Text="● 原调  " Foreground="#79DFC4"/>
                <TextBlock Text="● ↓ 降调  " Foreground="#7CB8FF"/>
                <TextBlock Text="● ↑ 升调  " Foreground="#FFB969"/>
                <TextBlock Text="▣ # 半音（粉边）" Foreground="#F58AE2"/>
            </WrapPanel>
            <StackPanel Grid.Row="2" Margin="0,0,0,8">
                <TextBlock x:Name="Hint" Foreground="#DEE8F0" FontSize="12" TextWrapping="Wrap"/>
                <TextBlock x:Name="PracticeText" Margin="0,4,0,0" Foreground="#C8D93A" FontSize="12" TextWrapping="Wrap" Visibility="Collapsed"/>
            </StackPanel>
            <local:NoteFallView x:Name="NotesView" Grid.Row="3"/>
            <Thumb x:Name="ResizeThumb" Grid.Row="3" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Width="18" Height="18" Cursor="SizeNWSE" DragDelta="ResizeOverlay" ToolTip="拖动调整大小">
                <Thumb.Template><ControlTemplate TargetType="Thumb"><TextBlock Text="◢" Foreground="#A8BAC7" Background="Transparent"/></ControlTemplate></Thumb.Template>
            </Thumb>
        </Grid>
    </Border>
</Window>
