<UserControl x:Class="DFH.Desktop.Views.LibraryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DFH.Desktop.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LibraryViewModel}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource SectionTitle}" Text="公开曲库" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Height="34" VerticalContentAlignment="Center"
                     Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="搜索歌名、作者、上传者或标签" />
            <Button Grid.Column="1" Margin="8,0,0,0" Height="34" Padding="12,0" Content="刷新" Command="{Binding RefreshCommand}" />
        </Grid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="10,0,10,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="46" />
                    <ColumnDefinition Width="46" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Style="{StaticResource Muted}" Text="歌名 · 作者 · 标签" FontSize="12" />
                <TextBlock Grid.Column="1" Style="{StaticResource Muted}" Text="上传者" FontSize="12" />
                <TextBlock Grid.Column="2" Style="{StaticResource Muted}" Text="时长" FontSize="12" TextAlignment="Right" />
                <TextBlock Grid.Column="3" Style="{StaticResource Muted}" Text="音符" FontSize="12" TextAlignment="Right" />
            </Grid>
            <ListBox Grid.Row="1" ItemsSource="{Binding Entries}" SelectedItem="{Binding Selected, Mode=TwoWay}"
                     MouseDoubleClick="OnDoubleClick" HorizontalContentAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="90" />
                                <ColumnDefinition Width="46" />
                                <ColumnDefinition Width="46" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Composer}" FontSize="11" Opacity="0.7" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding TagsLabel}" FontSize="11" Opacity="0.55" Margin="8,0,0,0" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding Uploader}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Opacity="0.85" />
                            <TextBlock Grid.Column="2" Text="{Binding DurationLabel}" VerticalAlignment="Center" TextAlignment="Right" Style="{StaticResource Mono}" />
                            <TextBlock Grid.Column="3" Text="{Binding NoteCount}" VerticalAlignment="Center" TextAlignment="Right" Style="{StaticResource Mono}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Style="{StaticResource Muted}" VerticalAlignment="Center" Text="{Binding Status}" TextTrimming="CharacterEllipsis" />
            <Button Grid.Column="1" Height="34" Padding="14,0" Content="打开选中" Command="{Binding OpenSelectedCommand}" />
        </Grid>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Height="34" VerticalContentAlignment="Center"
                     Text="{Binding ShortId, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="粘贴曲谱分享链接或 ID" />
            <Button Grid.Column="1" Margin="8,0,0,0" Height="34" Padding="12,0" Content="按 ID 打开" Command="{Binding OpenShortIdCommand}" />
        </Grid>
    </Grid>
</UserControl>
